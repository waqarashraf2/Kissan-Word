@extends('layouts.admin')
@section('title', $order->order_number)
@section('heading', 'Order Detail')
@section('content')
<div class="admin-page-heading"><div><h1>{{ $order->order_number }}</h1><p>Placed {{ $order->placed_at?->format('F d, Y h:i A') }}</p></div><a class="admin-secondary" href="{{ route('admin.orders.index') }}">Back</a></div>
<div class="admin-detail-grid"><section class="admin-card"><div class="admin-card-head"><div><h2>Items</h2></div></div><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Product</th><th>Price</th><th>Qty</th><th>Total</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td><strong>{{ $item->product_name }}</strong><small>{{ $item->sku }}</small></td><td>Rs. {{ number_format((float)$item->unit_price) }}</td><td>{{ $item->quantity }}</td><td>Rs. {{ number_format((float)$item->line_total) }}</td></tr>@endforeach</tbody></table></div><div class="admin-order-totals"><span>Subtotal <strong>Rs. {{ number_format((float)$order->subtotal) }}</strong></span><span>Shipping <strong>Rs. {{ number_format((float)$order->shipping_total) }}</strong></span><span>Total <strong>Rs. {{ number_format((float)$order->grand_total) }}</strong></span></div></section>
<aside class="admin-form-stack"><section class="admin-form-card"><h2>Customer</h2><p><strong>{{ $order->customer_name }}</strong><br>{{ $order->customer_phone }}<br>{{ $order->customer_email }}</p><p>{{ $order->shipping_address }}<br>{{ $order->city }}</p>@if($order->notes)<p><strong>Notes:</strong><br>{{ $order->notes }}</p>@endif</section><form class="admin-form-card" action="{{ route('admin.orders.update',$order) }}" method="POST">@csrf @method('PATCH')<h2>Update order</h2><label>Status<select name="status">@foreach(['pending','confirmed','processing','shipped','completed','cancelled'] as $status)<option value="{{ $status }}" @selected($order->status===$status)>{{ ucfirst($status) }}</option>@endforeach</select></label><label>Payment status<select name="payment_status">@foreach(['pending','paid','failed','refunded'] as $status)<option value="{{ $status }}" @selected($order->payment_status===$status)>{{ ucfirst($status) }}</option>@endforeach</select></label><button class="admin-primary">Update Order</button></form></aside></div>
@endsection
